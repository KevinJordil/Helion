<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Only used for the two Strava HTTPS calls this app ever makes (upload, refresh
         token) plus the OAuth authorization page opened in a browser/Custom Tab. Nothing
         else in Helion talks to the network. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Only needed on Android 8.0/8.1 (API 26 to 28): that is before MediaStore's Downloads
      collection existed, so the "save TCX to Downloads" action has no scoped storage route
      on those versions and must write a plain file into the public Downloads directory
      instead, see ch.kevinjordil.helion.strava.saveTcxToDownloads. maxSdkVersion means
      this permission is simply never requested, and never shows in the Play Store listing,
      on API 29+, where the MediaStore route needs no permission at all.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!--
      Package visibility (API 30+): needed to check whether the Strava app is installed
      (ch.kevinjordil.helion.strava.buildOpenStravaIntent) and, when it is not, whether the
      Play Store can handle a market:// listing before falling back to Strava's web upload
      page.
    -->
    <queries>
        <package android:name="com.strava" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
    </queries>

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:name=".HelionApp"
        android:label="@string/app_name"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Strava's OAuth redirect. The exact URI implemented is
              "helion://oauth-callback" (scheme "helion", host "oauth-callback", no path),
              matching the callback domain "helion" configured on the Strava app settings.
              singleTask above ensures the browser hands this intent back to the
              already-running MainActivity instance (onNewIntent) rather than spawning a
              second one.
            -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="helion" android:host="oauth-callback" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
          WorkManager's default androidx.startup initializer is removed here because
          HelionApp implements Configuration.Provider: WorkManager then initialises
          on demand, on the first WorkManager.getInstance() call (see SyncScheduler),
          instead of eagerly on process start with a default Configuration it would
          otherwise discard in favour of ours.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
